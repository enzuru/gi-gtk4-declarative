* 0.8.0
    - First release. Declarative libadwaita widgets on top of
      gi-gtk4-declarative: `IsBin` instances for the single-child
      widgets, the two toolbar-view slots, and a declarative
      `AdwTabView`.
